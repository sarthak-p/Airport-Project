#pragma once

#include <iostream>
#include <vector>

using namespace std;

class Graph {
    public:
    Graph(); 
    void addEdge(int departure, int arrival);
    

    private:

}

struct airportNode {

    airportNode * next;
};

struct routeEdge {
    string    
};

class Graph {
};